{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for all project windows",
  "windows": ["main", "main-*"],
  "permissions": [
    "core:default",
    "core:window:allow-destroy",
    "core:window:allow-set-title",
    "clipboard-manager:allow-write-text",
    "dialog:default"
  ]
}
